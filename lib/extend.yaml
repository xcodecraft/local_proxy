_mod :
    - !R.modul
        _name : "local_proxy"
        _args : "${L_BEND_SVC},${L_HOST},${L_CHECK_URL},${L_PORT}"
        _res  :
            - !R.file_tpl
                tpl : "${LOCAL_PROXY_HOME}/lib/std.vcl"
                dst : "${L_DST}/${PRJ_NAME}.vcl"
